Next is **not** another broad shell phase.

You have already built most of the shell:

* Search
* Discover
* People quick access
* Browse
* Filters
* Activity mini
* Empty state
* result-surface awareness

So the next highest-leverage step is:

# Next phase

## **UX-8, result quality and interaction polish**

This should have three subtracks, in this order:

### 1. People merge review workflow

You already created entry points for:

* possible merges
* unnamed clusters
* top identities

Now you need to make them actionable.

This is the most important next step because your People UX is now visible, but still not trustworthy enough. The shell can surface identities, but the user still cannot properly resolve:

* over-merged child faces
* split identities
* unnamed clusters
* suspicious clusters

So the next real product step is:

### **UX-8A, Merge Review**

Build:

* suspicious cluster list
* candidate pair list
* approve merge
* reject merge
* “not the same person”
* “same person”
* audit trail / review queue

That is what turns People from “interesting” into “usable”.

---

### 2. Result grid integration and refinement feedback

Your shell is now getting rich, but the result pane still needs to feel connected.

So after merge review:

### **UX-8B, Result pane interaction polish**

Build:

* visible search-state badges inside result pane
* contextual “why these results” hint
* better empty/loading/no-result states
* preserve previous results while searching
* skeleton loading instead of abrupt blank states
* smooth sort / filter / chip transitions

This is where Google/Apple feel polished and your app still feels transitional.

---

### 3. Facet quality and real orchestrator-backed refinements

Your current facets exist, but they are still early-stage.

So after result-pane polish:

### **UX-8C, Real facet intelligence**

Improve:

* People facet quality
* Location facet quality
* Year/type/favorites interactions
* preserve browse mode without confusing search mode
* make facet counts and active states stable
* expose only relevant filters for current family/query

That is when filters become truly Lightroom/Google-like instead of “present”.

---

# What I would **not** do next

Do **not** start another broad redesign phase like:

* rebuilding the whole Google layout again
* adding more shell sections just because the original plan listed them
* adding too many new widgets before making the current ones trustworthy

That would be a mistake.

You are no longer blocked by architecture.
You are now blocked by **interaction quality and decision workflows**.

---

# The new priority stack

## Priority 1

**People merge review**

Because your current clustering still produces:

* wrong merges
* swallowed identities
* unnamed clusters
* suspicious small clusters

And now the user can see People, but cannot truly fix them.

## Priority 2

**Result pane polish**

Because the shell is ahead of the pane right now.

## Priority 3

**Facet intelligence**

Because current facets are useful, but not yet first-class.

---

# Updated roadmap from here

## Completed enough

* UX-1, shell ownership
* UX-2A/2B, search bar, header, chips, hub
* UX-3, filters/facets foundation
* UX-4A/4B, discover cards, people quick access
* UX-5A, result-pane state awareness
* UX-6, browse + activity mini
* UX-7A, merge-review entry points + activity visibility wiring

## Next

### **UX-8A, People merge review**

Create:

* `ui/search/people_merge_suggestions_panel.py`
* suspicious cluster model
* merge/reject actions
* `MainWindow` bridge to People backend
* `people_section.py` review adapters

### Then

### **UX-8B, Result pane interaction polish**

Improve:

* loading states
* chip/result synchronization
* search explanation hints
* state-preserving refreshes

### Then

### **UX-8C, Facet intelligence**

Improve:

* orchestrator-backed facet counts
* family-aware filter exposure
* browse/search coexistence rules

---

# Why this is the right next move

Because your original plan was correct structurally, but the app has now reached a new stage.

Originally the problem was:

* missing architecture

Now the problem is:

* **trust**
* **usability**
* **correctability**

A user will forgive missing sections faster than they will forgive:

* wrong person grouping
* inability to fix grouping
* confusing results
* unstable facet behavior

That is the hard truth.

---

# My recommendation

The next implementation pack should be:

## **UX-8A, People merge review**

with:

* merge suggestion panel
* unnamed cluster review
* suspicious cluster review
* approve / reject actions
* data bridge to existing People backend

That is the highest-value next step.
